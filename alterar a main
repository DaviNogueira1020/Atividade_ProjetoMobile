import 'package:flutter/material.dart';
import 'package:firebase_core/firebase_core.dart';
import 'firebase_options.dart'; // Arquivo gerado automaticamente no Passo 3

void main() async {
  // Garante que o Flutter está pronto antes de chamar pacotes externos
  WidgetsFlutterBinding.ensureInitialized();
  
  // Inicia o Firebase com as configurações corretas para o seu app
  await Firebase.initializeApp(
    options: DefaultFirebaseOptions.currentPlatform,
  );

  runApp(MeuApp());
}
